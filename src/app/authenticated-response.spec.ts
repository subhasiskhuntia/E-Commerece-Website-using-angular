import { AuthenticatedResponse } from './authenticated-response';

describe('AuthenticatedResponse', () => {
  it('should create an instance', () => {
    expect(new AuthenticatedResponse()).toBeTruthy();
  });
});
