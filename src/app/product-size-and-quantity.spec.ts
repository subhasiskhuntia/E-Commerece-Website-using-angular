import { ProductSizeAndQuantity } from './product-size-and-quantity';

describe('ProductSizeAndQuantity', () => {
  it('should create an instance', () => {
    expect(new ProductSizeAndQuantity()).toBeTruthy();
  });
});
